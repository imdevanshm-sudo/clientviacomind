{
  "name": "client1homepage",
  "version": "1.0.0",
  "private": true,
  "description": "Viacom Production Hub multipage static site",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "node scripts/build.mjs",
    "preview": "npm run build && python3 -m http.server 4173 -d dist",
    "check:routes": "node scripts/verify-routes.mjs",
    "check:external": "node scripts/check-external-links.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
